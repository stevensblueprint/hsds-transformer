services:
  api:
    build:
      context: .
    container_name: openreferral-validator
    ports:
      - "8000:8000"
    environment:
      APP_MODULE: api.main:app
      PORT: "8000"
    restart: unless-stopped
